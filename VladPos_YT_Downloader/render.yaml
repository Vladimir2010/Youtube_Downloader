services:
  - type: web
    name: vladpos-yt-downloader
    env: python
    buildCommand: "./render_build.sh"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
