# 🎥 YouTube Downloader (Educational Project)

Това е напълно функционално уеб приложение за изтегляне на видео и аудио от YouTube, създадено с Python (Flask) и JavaScript.

## 🚀 Как работи архитектурата?
1.  **Frontend**: Клиентът въвежда линк. Браузърът изпраща заявка към сървъра.
2.  **Backend (Flask)**: Сървърът използва библиотеката `yt-dlp`, за да извлече информация за видеото.
3.  **Background Processing**: Тъй като свалянето отнема време, процесът се стартира във фонов режим (background thread).
4.  **Polling**: Браузърът пита сървъра на всеки 2 секунди: "Готов ли си?".
5.  **FFmpeg**: Ако е необходимо обединяване на видео и аудио (за висока резолюция), се използва `ffmpeg`.

---

## 🛠 Локално стартиране (Windows)

1.  **Инсталирайте Python 3.11+** от [python.org](https://www.python.org/).
2.  **Инсталирайте FFmpeg**:
    *   Изтеглете от [gyan.dev](https://www.gyan.dev/ffmpeg/builds/).
    *   Добавете `bin` папката в PATH на Windows.
3.  **Инсталирайте зависимостите**:
    ```bash
    pip install -r requirements.txt
    ```
4.  **Стартирайте сървъра**:
    ```bash
    python app.py
    ```
5.  Отворете `http://127.0.0.1:5000` в браузъра.

---

## ☁️ Deploy в Render.com (Безплатно)

### 1. Подготовка на GitHub
*   Качете всички файлове от проекта в ново публично хранилище (Repository) в GitHub.

### 2. Създаване на Render акаунт
*   Отидете на [Render.com](https://render.com/) и се впишете с GitHub акаунта си.

### 3. Нов Web Service
*   Натиснете **New+** -> **Web Service**.
*   Свържете вашия GitHub Repository.
*   **Name**: `my-youtube-downloader` (или нещо уникално).
*   **Environment**: `Python 3`.
*   **Region**: Изберете най-близкия (Frankfurt).
*   **Build Command**: Вече е настроен в `render.yaml`, но ако правите ръчно:
    ```bash
    apt-get update && apt-get install -y ffmpeg && pip install -r requirements.txt
    ```
*   **Start Command**: `gunicorn app:app`.

### ⚠️ Ограничения на Free Plan (Render)
*   **RAM (512MB)**: При много големи видеа сървърът може да рестартира.
*   **Disk**: Файловете са временни. Имаме вграден скрипт в `app.py`, който ги трие автоматично след 1 час, за да не препълва мястото.
*   **Spin down**: Ако сайтът не се ползва 15 минути, той "заспива". Първото отваряне след това ще отнеме ~30 секунди.

---

## 🎓 Как да представите проекта
Това е перфектен учебен проект, защото демонстрира:
1.  **Full-stack разработка**: Комуникация между JS и Python.
2.  **Асинхронни задачи**: Как сървърът не "зависва" по време на тежка работа.
3.  **Работа с външни инструменти**: Интеграция с CLI инструменти като `yt-dlp` и `ffmpeg`.
4.  **Cloud Deployment**: Умение за качване на реално работещ софтуер в облака.

> [!IMPORTANT]
> Проектът е създаден единствено за образователни цели. Спазвайте авторските права и условията на YouTube.
